using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpringBehavior : MonoBehaviour
{
    //[SerializeField] float SpringForce = 10f;
    //Vector3 SpringLauncher = (0, SpringForce);
    private void SpringCollision(Collision collision)
    {
        //if (collision.gameObject.tag == "Player")
        //{
        //    PlayerController Player = new PlayerController();
        //    Player.JumpIncrease();
        //    Debug.Log("Your jump height has increase");


        //}
    }
}
